import { IndustryPage } from '@/components/IndustryPage';
import { k12PTAsData } from '@/data/serveIndustries';

const K12PTAs = () => {
  return <IndustryPage data={k12PTAsData} />;
};

export default K12PTAs;